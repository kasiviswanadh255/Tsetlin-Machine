Hardware implementation of Tsetlin Machine for handwrtten digit recognition.
Designed to recognize 2 digits but can be extended wuth more exclide states and updating its thresold to detect multiple.
Uses MNIST dataset as input features.
